# Google Sign-In (OAuth web client — public ID, safe to expose in the browser)
# Create credentials: https://console.cloud.google.com/apis/credentials
# Add Authorized JavaScript origins: http://localhost:3000  (and your deployed URL)
REACT_APP_GOOGLE_CLIENT_ID=
